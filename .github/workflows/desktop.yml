name: Simple Workflow

on:
  push:
    branches: 
      - main
jobs:        
  hello-world:
    runs-on: ubuntu-latest
        
    steps:
      - uses: actions/checkout@v4
      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x
      - name: 🔎 Check IP
        run: curl https://api.ipify.org
      - name: 🔎 Check IPlocal
        run: curl http://localhost:5009
      - name: start server
        timeout-minutes: 1
        run: dotnet run

        
  hello:
    runs-on: ubuntu-latest

    steps:
      - name: q52
        run: echo "qwe"
        timeout-minutes: 1
      
