name: Simple Workflow

on:
  push:
    branches: 
      - main
jobs:        
  hello-world:
    runs-on: ubuntu-latest
        
    steps:
      - name: chmod
        run: chmod +x hello.txt
      - name: git push
        run: touch qwe.txt
      - name: qwe
        run: echo "qwe">>qwe.txt
      - name: git
        run: ./qwe.txt

      
